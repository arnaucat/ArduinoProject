#include "Crane.h"

void Crane::deliverMail(Mail* mail)
{
       /*
	Serial.print("CRANE: Delivering mail to user ");
	Serial.print(mail->getUser()->getId());
	Serial.print("from box at x:")
	Serial.println(mail->getBox()->getX());
	Serial.print(" y:")
	Serial.println(mail->getBox()->getY());
	*/
}
void Crane::storeMail(Mail* mail)
{
    /*
	Serial.print("CRANE: Storing mail to user ");
	Serial.print(mail->getUser()->getId());
	Serial.print("in box at x:")
	Serial.println(mail->getBox()->getX());
	Serial.print(" y:")
	Serial.println(mail->getBox()->getY());
	*/
}

